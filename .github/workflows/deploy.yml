name: Portfolio Workflow

on:
  push:
    branches:
      - main

jobs:
  check-project:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: List project files
        run: ls -la

      - name: Success message
        run: echo "Portfolio workflow success!"